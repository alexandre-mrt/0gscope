{
  "name": "0gscope",
  "version": "0.1.0",
  "description": "AI-powered analytics dashboard for the 0G network. Track chain activity, storage usage, and compute metrics.",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
